function SettingsModal() {
  return (
    <div className="modal">
      {/* Formulare pentru schimbare font, etc. */}
      <button
        onClick={() => {
          /* Logica schimbare font */
        }}
      >
        Schimbă Font
      </button>
      {/* ... Alte elemente de formular ... */}
    </div>
  );
}

export default SettingsModal;
